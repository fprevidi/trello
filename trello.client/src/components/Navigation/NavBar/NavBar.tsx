import React from 'react';
import './NavBar.css';

const Navbar: React.FC = () => {
    return (
        <nav className="navbar">
            <h1>My Trello Clone</h1>
            {/* Aggiungi altri elementi di navigazione qui */}
        </nav>
    );
};

export default Navbar;
